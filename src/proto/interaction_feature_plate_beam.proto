syntax = "proto3";

package wood_proto;

// The state of an InteractionFeaturePlateBeam beyond the kernel Interaction; nothing computed yet.
message InteractionFeaturePlateBeam {
  string contact_guid = 1;  // Guid of the contact this was solved from; empty when none
}
