# Wood

The timber joinery solver: plates, beams, columns and blocks on the session kernel, the contacts and joints between them stored as records on the graph, and a protobuf file the viewer opens as a plain Session.

The pages on the left take the structure below section by section, each with a diagram and a pointer into the API. The text below is `src/docs.md`, the one-page description kept in the source tree.

---

--8<-- "src/docs.md"
